import 'package:flutter/material.dart';

class AppPallete {
  // * News App Theme
  static const newsTheme = {
    'primary': Color(0xFF1E88E5), // * Professional blue
    'primaryLight': Color(0xFF6AB7FF), // * Lighter blue
    'primaryDark': Color(0xFF005CB2), // * Darker blue
    'accent': Color(0xFFE53935), // * Attention-grabbing red
    'accentLight': Color(0xFFFF6F60), // * Lighter red
    'accentDark': Color(0xFFAB000D), // * Darker red
    'background': Color(0xFFF7F7F7), // * Neutral light background
    'surface': Colors.white, // * Clean white surface
    'textPrimary': Color(0xFF212121), // * Deep gray for primary text
    'textSecondary': Color(0xFF757575), // * Medium gray for secondary text
    'success': Color(0xFF4CAF50), // * Green for success states
    'warning': Color(0xFFFFC107), // * Amber for warnings
    'error': Color(0xFFD32F2F), // * Soft red for errors
    'inputBackground': Color(0xFFFFFFFF), // * Plain white for inputs
    'inputBorder': Color(0xFFB0BEC5), // * Light blue-gray border
    'inputFocusBorder': Color(
      0xFF1E88E5,
    ), // * Use primary color for focus border
  };
}
